#!/usr/bin/env bash
# Host-neutral fallback profile. Model- and hardware-specific values are
# supplied by the Docker task environment or planner candidate.
PROFILE_ID="AUTO"
PROFILE_DESCRIPTION="Portable automatic profile"
MODEL_PATH="${MODEL_PATH:-/model}"
CHECKPOINT_KIND="${CHECKPOINT_KIND:-generic}"
SERVED_MODEL_NAME="${SERVED_MODEL_NAME:-local-model}"
TOOL_CALL_PARSER="${TOOL_CALL_PARSER:-}"
REASONING_PARSER="${REASONING_PARSER:-}"
DEFAULT_CHAT_TEMPLATE_KWARGS="${DEFAULT_CHAT_TEMPLATE_KWARGS:-}"
if [[ -z "$DEFAULT_CHAT_TEMPLATE_KWARGS" ]]; then
  DEFAULT_CHAT_TEMPLATE_KWARGS='{}'
fi
TP_SIZE="${TP_SIZE:-1}"
DP_SIZE="${DP_SIZE:-1}"
PP_SIZE="${PP_SIZE:-1}"
ENABLE_DP_ATTENTION="${ENABLE_DP_ATTENTION:-0}"
ENABLE_DP_LM_HEAD="${ENABLE_DP_LM_HEAD:-0}"
ENABLE_DSPARK="${ENABLE_DSPARK:-0}"
MOE_RUNNER_BACKEND="${MOE_RUNNER_BACKEND:-}"
MOE_A2A_BACKEND="${MOE_A2A_BACKEND:-}"
QUANTIZATION="${QUANTIZATION:-}"
